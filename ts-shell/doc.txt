after clone have to build the lib first
wasm-pack build --dev --out-dir ts-shell/lib/wgputoy
as we are using wasm-plugin in vite the import in \ts-shell\src\wgputoyatoms.tsx
 will load the wasm and call the init automatically

atom used

canvasParentElAtom
  set in App,
  used in wgputoyAtom getter before it call create_renderer (it check app is init)

canvasElAtom
  set in WgpuToyWrapper refere to the canvas and checked that canvas support gpu

wgputoyAtom used in Controller, check canvasElAtom, canvasParentElAtom